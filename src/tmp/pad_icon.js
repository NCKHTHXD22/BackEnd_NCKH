import puppeteer from "puppeteer"; import fs from "fs"; import path from "path"; const imgPath = path.resolve("../mobile/assets/images/Logo.png"); const outPath = path.resolve("../mobile/assets/images/AdaptiveIcon.png"); const imgData = fs.readFileSync(imgPath).toString("base64"); const html = `<html><body style="margin:0;padding:0;background:transparent;width:1024px;height:1024px;display:flex;align-items:center;justify-content:center;"><img style="width:624px;height:624px;object-fit:contain;" src="data:image/png;base64,${imgData}" /></body></html>`; (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); await page.setViewport({width: 1024, height: 1024, deviceScaleFactor: 1}); await page.setContent(html); await page.evaluate(() => document.body.style.background = "transparent"); await page.screenshot({path: outPath, omitBackground: true, clip: {x:0, y:0, width:1024, height:1024}}); await browser.close(); console.log("Done padding icon"); })();
