node_modules
.env
tmp/
venv/
__pycache__/
*.pyc
.vscode/
dataset_*.npy
# Note: artifacts/*.pt and *.pkl are intentionally tracked (model weights for HuggingFace deploy)

# Regenerable per-lake training tensors (run train_4_lake/prepare_dataset.py to recreate, ~5.6GB)
LSTM_Py_Backend/lstm_service/train_4_lake/data/

# Raw input spreadsheets, not code
LSTM_Hue/Data_Lake_Rain/

# Regenerable per-reservoir v2 datasets (run main_build_dataset.py --all --legacy-v1, ~25GB)
LSTM_Py_Backend_v2/datasets/
LSTM_Py_Backend_v2/datasets_all_reservoirs.zip

# Kaggle CLI upload staging (hardlink to the zip above, same 25GB content -- see
# _kaggle_upload/dataset-metadata.json)
_kaggle_upload/

# Large archives and tree-based model JSON/pkl weights (deployed via SCP / direct storage)
XGBoost_Py_Backend/result_xgboots.zip
XGBoost_Py_Backend/artifacts/
RF_Py_Backend/artifacts/

