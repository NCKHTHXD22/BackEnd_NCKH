# ─── Core framework ───────────────────────────────────────────────────────────
fastapi
uvicorn[standard]
pydantic
gunicorn

# ─── Data processing ──────────────────────────────────────────────────────────
numpy
pandas
scikit-learn
joblib
openpyxl

# ─── XGBoost (tabular model — reg:quantileerror cần >=2.0) ───────────────────
xgboost>=2.0

# ─── HTTP requests ────────────────────────────────────────────────────────────
requests
httpx

# ─── PyTorch (CPU-only build — smaller Docker image, HuggingFace free tier) ──
# Full CUDA build is ~3GB; CPU-only is ~800MB and sufficient for inference
--extra-index-url https://download.pytorch.org/whl/cpu
torch

# ─── Utilities ────────────────────────────────────────────────────────────────
python-dotenv
tqdm